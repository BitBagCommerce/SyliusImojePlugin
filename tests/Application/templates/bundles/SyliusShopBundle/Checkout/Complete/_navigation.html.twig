{% set init_path = path('bitbag_ing_pay_initialize_payment') %}
{% set factory_name = constant('BitBag\\SyliusIngPayPlugin\\Provider\\IngPayClientConfigurationProviderInterface::FACTORY_NAME') %}

{% if order.payments.last.method.gatewayConfig.factoryName != factory_name %}
    <button type="submit" class="ui huge primary fluid icon labeled button" {{ sylius_test_html_attribute('confirmation-button') }}>
        <i class="check icon"></i> {{ 'sylius.ui.place_order'|trans }}
    </button>
{% elseif order.payments.last.details | join('|') == "card" and order.payments.last.details | keys | join('|') == "ingPayPaymentMethods" %}
    <button type="button" class="ui huge primary fluid icon labeled button js-widget-ing-pay-action">
        <i class="check icon"></i> {{ 'sylius.ui.place_order'|trans }}
    </button>
{% else %}
    <button
            type="submit"
            class="ui huge primary fluid icon labeled button"
            {{ sylius_test_html_attribute('confirmation-button') }}
            id="submitPayment"
            data-init-payment-path="{{ init_path }}">
        <i class="check icon"></i> {{ 'sylius.ui.place_order'|trans }}
    </button>
{% endif %}
